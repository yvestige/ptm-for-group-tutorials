#include <stdio.h>
#include <stdlib.h>

/* This code shows how a linear run works.
   This time, with a conditional statement for exiting. */

// Variable declarations
int num_variable = 0;
const int exit_value = 10;

// Start of executing
int main() {

    // Iteration 1
    num_variable += 1;
    printf("Iteration 1: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 2
    num_variable += 1;
    printf("Iteration 2: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 3
    num_variable += 1;
    printf("Iteration 3: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 4
    num_variable += 1;
    printf("Iteration 4: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 5
    num_variable += 1;
    printf("Iteration 5: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 6
    num_variable += 1;
    printf("Iteration 6: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 7
    num_variable += 1;
    printf("Iteration 7: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 8
    num_variable += 1;
    printf("Iteration 8: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 9
    num_variable += 1;
    printf("Iteration 9: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 10
    num_variable += 1;
    printf("Iteration 10: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 11
    num_variable += 1;
    printf("Iteration 11: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 12
    num_variable += 1;
    printf("Iteration 12: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 13
    num_variable += 1;
    printf("Iteration 13: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 14
    num_variable += 1;
    printf("Iteration 14: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 15
    num_variable += 1;
    printf("Iteration 15: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 16
    num_variable += 1;
    printf("Iteration 16: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 17
    num_variable += 1;
    printf("Iteration 17: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 18
    num_variable += 1;
    printf("Iteration 18: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 19
    num_variable += 1;
    printf("Iteration 19: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // Iteration 20
    num_variable += 1;
    printf("Iteration 20: Current value of num_variable: %d\n", num_variable);

    // Check if done?
    if (num_variable == exit_value) {
        exit(0);
    }

    // End of code
    return 0;
}
